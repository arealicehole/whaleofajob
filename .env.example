# Netlify Environment Variables for Contact Form
# Copy this file to .env.local for local testing
# Set these same variables in Netlify Dashboard > Site Settings > Environment Variables

# SendGrid SMTP Configuration
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=your_sendgrid_api_key_here

# Email Configuration
MAIL_FROM="Yzagere Enterprises" <noreply@yzagere.com>
MAIL_TO=yzagere.enterprises@gmail.com

# Alternative: Gmail SMTP (if not using SendGrid)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-specific-password